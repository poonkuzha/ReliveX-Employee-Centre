services:
  - type: web
    name: relivex-frontend
    env: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: frontend/dist/frontend
    routes:
      - type: rewrite
        source: /*
        destination: /index.html